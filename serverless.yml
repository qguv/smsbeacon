service: smsbeacon

provider:
  name: aws
  runtime: python3.6

plugins:
  - serverless-wsgi

functions:
  app:
    handler: wsgi.handler
    events:
      - http: ANY {proxy+}
      - http: ANY /

custom:
  wsgi:
    app: app.app
